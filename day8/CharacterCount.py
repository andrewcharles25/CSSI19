def count_chars():
    word = raw_input("Enter a string\n")
    d = {}
    for i in word:
		if d.has_key(i):
             d[i] = d[i]+1
        else
             d[i]=1
            for x,y in d.items():        
print x, ":" ,y

